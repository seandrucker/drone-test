workspace:
  base: /go
  path: src/github.com/sundryp/drone-test
  
pipeline:
  build:
    image: gcc
    commands:
      - ls -al
      - echo "hello" > hello.txt
      - pwd

  build2:
    image: gcc
    commands:
      - ls -al
      - pwd

